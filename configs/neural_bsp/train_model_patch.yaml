dataset:
  root: G:\Dataset\GSP_v3_part\total_1k.hdf5
#  root: F:\GSP\GSP_v3\training
#  dataset_name: ABC_dataset_patch_hdf5
  dataset_name: ABC_dataset_patch

trainer:
  learning_rate: 1e-4
  gpu: 1
  num_worker: 4

  batch_size: 128
  resume_from_checkpoint:
  evaluate: false
  output: .
  check_val_every_n_epoch: 1

model:
  model_name: Base_model_full
  phase: 0

hydra:
  run:
    dir: ./outputs/${now:%Y_%m_%d_%H_%M_%S}
  job:
    chdir: False
#  output_subdir: null
#  job_logging: disabled
#  hydra_logging: disabled